第5章 列表和元组

    在Python中，序列(sequence)是最基本的结构。Python中内置了5个常用的序列结构，
分别是列表、元组、集合、字典和字符串。

5.1 序列概述

    序列是一块用于存储多个值的连续内存空间，并且按一定顺序排列，每个值（称为元
素）都分配一个数字，称为索引位置。

5.1.1 索引(index)

    序列中的每个元素都有一个编号，也称为索引(index)。

5.1.2 切片(slice)

    切片操作是访问序列中元素的另和种方法，它可以访问一定范围内的元素。通过切片
操作可以生成一个新的序列。实现切片操作的语法格式如下：sname[start:end:step]。

5.1.3 序列相加

    在Python中，支持两种相同类型的序列相加(addition)操作，即将两个序列进行连接，
不会去除重复的元素，使用加(+)运算符实现。

5.1.4 乘法

    在Python中，使用数字n乘以一个序列会生成新的序列。

5.1.5 检查某个元素是否是序列的成员

    在Python中，可以使用in关键字检查某个元素是否是序列的成员，即检查某个元素是
否包含在该序列中。

    value in sequence

5.1.6 计算序列的长度、最大值和最小值

    在Python中，提供了内置函数计算序列的长度、最大值和最小值。分别是使用len()
函数计算序列的长度，即返回序列包含多少个元素；使用max()函数返回序列中的最大元
素；使用min()函数返回序列中的最小元素。

--------------------------------------------------------------------------------
5.2 列表

    Python中的列表是由一系列按特定顺序排列的元素组成。它是Python中内置的可变序
列。在形式上，列表的所有元素都放在一对中括号"[]"中，两个相邻元素间使用逗号","分
隔。在内容上，可以将整数、实数、字符串、列表、元组等任何类型的内容放入列表中，
并且同一个死忠中，元素的类型可以不同。

5.2.1 列表的创建的删除

5.2.2 访问列表元素

5.2.3 遍历列表

5.2.4 添加、修改和删除列表元素

    1.添加元素

    列表对象的append()方法用于列表的末尾追加元素，语法格式：listname.append(obj)

    2.修改元素

    3.删除元素

    (1)根据索引删除

    删除列表中的指定元素和删除列表类似，可以使用del语句实现。不同的是在指定列表
名称时，换为列表索引。

    (2)根据元素值删除

    如果想要删除一个不确定位置的元素，可以使用列表对象的remove()方法实现。

5.2.5 对列表进行统计计算

    1.获取指定元素的出现次数

    listname.count(obj)

    2.获取指定元素首次出现的下标

    listname.index(obj)

    3.统计数值列表的元素和

    sum(iterable[,start])

5.2.6 对列表进行排序

    1.使用列表对象的sort()方法实现

    listname.sort(key=None, reverse=False)

    2.使用内置的sorted()函数实现

    sorted(iterable, key=None, reverse=False)

    使用sort()方法时，会改变原列表的元素排列顺序，使用sorted()函数时，会建立一
个列表副本。

5.2.7 列表推导式

    使用列表推导式可以快速生成一个列表，或者根据某个列表生成满足指定需求的列表。

    (1)生成指定范围的数值列表
    
    list = [Expression for var in range]

    (2)根据列表生成指定需求的列表

    newlist = [Expression for var in list]

    (3)从列表中选择符合条件的元素组成新的列表

    newlist = [Expression for var in list if condition]

5.2.8 二维列表

--------------------------------------------------------------------------------
5.3 元组

    元组(tuple)是Python中的另一个重要的序列结构，与列表类似，也是一系列特定顺序
排列的元素组成。但是，它为不可变序列。在形式上，元组的所有元素都放在一对小括号
"()"中，两个相信元素间使用逗号","分隔；在内容上，可以将整数、实数、字符串、列
表、元组等任何类型的内容放入元组中，并且同一个元组中，元素的类型可以不同，因为
它们之间没有任何关系。

5.3.1 元组的创建和删除

    1.使用赋值运算符直接创建元组

    如果创建元组只包含一个元素，则需要在定义元组时，在元素的后面加一个逗号","。

    2.创建空元组

    3.创建数值元组

    3.删除元组

5.3.2 访问元组元素

5.3.3 修改元组

    元组是不可变序列，所以不能对它的单个元素值进行修改，但是元组并不是完全不可
以修改，可以对元组进行重新赋值。

5.3.4 元组推导式

5.3.5 元组与列表的区别

    (1)列表属于可变序列，而元组属于不可变序列。

    (2)列表可以使用append()、extend()、insert()、remove()和pop()等方法，而元组
       不可以修改。

    (3)列表可以使用切片访问和修改列表中的元素。元组支持切片，但不支持修改。

    (4)元组比列表的访问和处理速度快。

    (5)列表不能作为字典的键，而元组可以。